<%= simple_form_for(@order_info) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :application_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
