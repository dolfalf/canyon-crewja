<%= simple_form_for(@item_group) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :item_group_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
